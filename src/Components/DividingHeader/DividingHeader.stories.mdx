import { Canvas, Meta, Story, Props } from '@storybook/addon-docs';
import { DividingHeader } from './DividingHeader';
import { Screen } from './../Screen';

export const Template = (args) => (
    <Screen>
        <DividingHeader {...args} />
    </Screen>
);

<Meta title="Components/DividingHeader" component={DividingHeader} />

# DividingHeader

...

## Examples

### Default

...

<Canvas>
    <Story name="Default" args={{}}>
        {Template.bind({})}
    </Story>
</Canvas>

leftTitle: PropTypes.string,
centerTitle: PropTypes.string,
rightTitle: PropTypes.string,

<Canvas>
    <Story name="Header Title" args={{}}>
        <Screen>
            <DividingHeader leftTitle="Left Title" />
            <DividingHeader centerTitle="Center Title" />
            <DividingHeader rightTitle="Right Title" />
        </Screen>
        <br />
        <p>Multiple texts can be used on the same header.</p>
        <Screen>
            <DividingHeader leftTitle="Left Title" rightTitle="Right Title" />
        </Screen>
    </Story>
</Canvas>

## API

<Props of={DividingHeader} />
