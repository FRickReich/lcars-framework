import { Canvas, Meta, Story, Props } from '@storybook/addon-docs';
import { DividingHeader } from './DividingHeader';
import { Screen } from './../Screen';

export const Template = (args) => (
    <Screen>
        <DividingHeader {...args} />
    </Screen>
);

<Meta title="Components/DividingHeader" component={DividingHeader} />

# DividingHeader

...

## Examples

### Default

...

<Canvas>
    <Story name="Default" args={{}}>
        {Template.bind({})}
    </Story>
</Canvas>

## API

<Props of={DividingHeader} />
